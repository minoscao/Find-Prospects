import test from 'node:test';import assert from 'node:assert/strict';import {assessCustomer} from '../cloudflare/assessment-api.js';
test('assessment requires rules and source evidence',async()=>{assert.equal((await assessCustomer({},{})).status,503);assert.equal((await assessCustomer({id:'1',name:'A',sources:[]},{AI:{},SKILL_STORE:{get:async()=> 'private'}})).status,422);});
test('assessment loads private rules and returns versions without rules',async()=>{let prompt='';const env={SKILL_STORE:{get:async()=> 'private-strategy-123'},AI:{run:async(m,p)=>{prompt=p.messages[0].content;return {response:'Assessment'};}}};const input={id:'1',name:'A',sources:[{url:'https://example.com',text:['Parties']}]};const r=await assessCustomer(input,env);assert.equal(r.status,200);assert.match(prompt,/private-strategy-123/);assert.equal(r.body.customerId,'1');assert.equal(r.body.ruleVersion.length,64);assert.ok(!JSON.stringify(r.body).includes('private-strategy-123'));const r2=await assessCustomer(input,env);assert.equal(r.body.ruleVersion,r2.body.ruleVersion);});
